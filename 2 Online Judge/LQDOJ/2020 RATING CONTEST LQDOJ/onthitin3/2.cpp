#include<bits/stdc++.h>
using namespace std;

int a[1000005];

int main() {
    freopen("input.txt", "r", stdin);
    ios::sync_with_stdio(0);
    cin.tie(NULL);
    string a,b;
    string c,d;
    getline(cin,a);
    getline(cin,b);
    int vtmax = 0, vtmin = 0;
    int sa = a.size(), sb= b.size();
    for (int i = 0; i < sb; i++) {
    	int q,e;
    	valb = b[i] - '0';
    	a1 = a[vtmax] - '0';
    	if (vtmax > sa) break;
    	//a2 = a[vtmin] - '0';
    	if (valb >= a1) {

    	}
    	else {
    		c.insert(i,a[vtmax]);
    		vtmax++ 
    	}
    }
    while (vtmax <= sa) insert
    return 0;
}